# Provider appearance

The provider portal uses one fixed **Professional Blue** appearance across desktop, tablet and mobile. Providers cannot change the portal theme. This keeps the interface consistent for training, support and screenshots while preserving semantic success, warning and error colours.
