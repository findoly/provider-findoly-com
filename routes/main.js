const router=require('express').Router();const{apiAuth}=require('../middleware/auth');router.use('/auth',require('./auth'));router.use(apiAuth);router.get('/dashboard',require('../controllers/dashboardController').get);router.get('/profile',require('../controllers/profileController').get);router.use('/lead',require('./lead'));router.use('/leads',require('./lead'));router.use('/wallet',require('./wallet'));module.exports=router;
